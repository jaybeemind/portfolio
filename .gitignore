# Agent and skill definitions — local tooling, not part of the site
.claude/agents/
.claude/skills/
.agents/

# Build-time tooling deps (tools/ only; the site itself ships no dependencies)
node_modules/

# Credentials for tools/capture-rooc.mjs — never commit
.env.local
.env
